const head_list = document.getElementById('head');





// when screen size resches to 600px
window.addEventListener('resize',function(e)
{
    if(e.target.width==='600px')
    {
        head_list.removeChild()
    }
});
